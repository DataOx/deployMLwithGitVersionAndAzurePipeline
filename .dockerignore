node_modules
npm-debug.log
Dockerfile*
docker-compose*
.dockerignore
.git
.gitignore
README.md
LICENSE
.vscode
__pycache__
env

./data/*
docs
.pipelines
.pytest_cache
.vscode
htmlcov
test
.coverage
coverage.xml
Pipfile
Pipfile.lock
README.md
version.json


# Ignore a file or directory in the context root named "data"
data

# Ignore any files or directories within the subdirectory named "data" 
# in the context root
data/*

# Ignore any files or directories in the context root beginning with "data"
data*

# Ignore any files or directories one level down from the context root named
# "data"
*/data

# Ignore any files or directories at any level, including the context root, 
# named data
**/data

# Ignore every file in the entire build context (see next rule for how this 
# could be used)
#*

# Re-include the file or directory named "src" that may have been previously
# excluded. Note that you cannot re-include files in subdirectories that have 
# been previously excluded at a higher level
!src
